<div align="right">

**فارسی** | [English](README.en.md)

</div>

---

# نصب یک دستوری ایستگاه سایفون (Conduit) v2.0

<div dir="rtl">

**نصب آسان پل (Bridge) سایفون با Docker و مدیریت هوشمند برای کمک به آزادی اینترنت در ایران**

[رامتین رحمانی‌نژاد - نت ادمین پلاس](https://netadminplus.com)

[یوتیوب](https://youtube.com/@netadminplus) • [وبسایت](https://netadminplus.com) • [اینستاگرام](https://instagram.com/netadminplus)

---

🕯️ **به یاد ۱۸ و ۱۹ دی‌‌ماه ۱۴۰۴**

روزهایی که صدای حق‌خواهی مردم ایران با گلوله پاسخ داده شد و ارزش متصل ماندن بیشتر از هر زمان دیگری درک شد.

> **"اگه چراغ رو خاموش می‌کنی و پرده‌ها رو می‌کشی، خودت می‌دونی کاری که می‌کنی درست نیست. کسی که کارش درسته، از روشنایی نمی‌ترسه."**

---

🔴 **وضعیت فعلی (بهمن ۱۴۰۴)**

دسترسی به اینترنت در ایران با محدودیت‌های شدیدی روبروست و بسیاری از کاربران در بن‌بست ارتباطی قرار گرفته‌اند. امروز بیش از هر زمان دیگری، مشارکت شما برای باز نگه داشتن روزنه‌های ارتباطی حیاتی است:

- **نیاز به سرور خارج از کشور:** برای کمک به این جریان، به یک سرور مجازی (VPS) خارج از ایران نیاز دارید.
- **نصب فقط با یک دستور:** نیازی به دانش پیچیده فنی نیست؛ شما می‌توانید با اجرای یک اسکریپت و فقط با یک دستور، نود Conduit خود را راه اندازی کنید.
- **تأثیر مستقیم:** هر سروری که اضافه شود، مستقیماً توان شبکه‌ی سایفون را برای عبور از سد فیلترینگ افزایش می‌دهد.
- **درباره انتخاب سرور:** برای کاهش هزینه، از سرورهایی با ترافیک نامحدود یا ترافیک ماهانه زیاد استفاده کنید. مثل Hetzner، BlueVPS، HostVDS، Contabo و ...

---

## امکانات نسخه ۲.۰ 🆕

### امکانات اصلی (نسخه ۱.۰)
- ✅ نصب با یک دستور (کاملاً خودکار)
- ✅ فرمان اختصاصی `conduit` برای مدیریت راحت از هر جای سیستم
- ✅ گزارش‌دهی زنده از تعداد کاربران و حجم ترافیک
- ✅ پایداری بالا با تنظیم محدودیت رم (RAM Limit)
- ✅ شروع به کار خودکار (Auto-restart) پس از ریبوت سرور
- ✅ بهینه شده برای سرورهای با منابع محدود (Low-End VPS)

### امکانات جدید نسخه ۲.۰ 🌟
- 🆕 **ردیابی تاریخچه:** ذخیره خودکار آمار هر ساعت تا ۹۰ روز گذشته
- 🆕 **نمودارهای روندی:** مشاهده تغییرات ۷ روز اخیر به صورت گرافیکی
- 🆕 **گزارش تأثیرگذاری:** خلاصه‌ای از کمک شما به آزادی اینترنت
- 🆕 **خروجی CSV:** دانلود آمار برای تحلیل‌های دقیق‌تر
- 🆕 **اعلان‌های تلگرام:** دریافت گزارش روزانه و هشدارها (اختیاری)
- 🆕 **رابط کاربری بهبود یافته:** نمایش زیباتر و حرفه‌ای‌تر اطلاعات

---

## پیش‌نیازها

**سخت‌افزار:**
- حداقل 512MB رم (سبک و بهینه)
- حداقل 5GB فضای خالی

**نرم‌افزار:**
- Ubuntu 20.04+، Debian 10+
- دسترسی root یا sudo
- **مهم:** سرور باید حتماً خارج از ایران باشد.

---

## نصب

### نصب یک خطی (پیشنهادی)

</div>

```bash
curl -fsSL https://raw.githubusercontent.com/netadminplus/conduit-docker/main/install.sh | sudo bash
```

<div dir="rtl">

### روش جایگزین: دانلود و اجرا

</div>

```bash
curl -fsSL https://raw.githubusercontent.com/netadminplus/conduit-docker/main/install.sh -o install.sh
chmod +x install.sh
sudo ./install.sh
```

<div dir="rtl">

**نکته:** در طول نصب، از شما سوال می‌شود که آیا می‌خواهید اعلان‌های تلگرام را فعال کنید یا خیر. این مرحله اختیاری است و می‌توانید بعداً هم آن را تنظیم کنید.

---

## مدیریت ایستگاه (دستورات Conduit)

بعد از نصب، از هر جای سیستم می‌توانید با دستور اختصاصی `conduit` ایستگاه را مدیریت کنید:

### دستورات اصلی

</div>

```bash
conduit report      # مشاهده گزارش زنده کاربران و ترافیک
conduit logs        # مشاهده لاگ‌های سیستمی و وضعیت اتصال
conduit down        # متوقف کردن ایستگاه
conduit up          # راه‌اندازی مجدد ایستگاه
conduit restart     # بازنشانی سریع سرویس
conduit help        # راهنمای دستورات
```

<div dir="rtl">

**مثال خروجی `conduit report`:**

</div>

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Psiphon Conduit Live Report
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟢 Status: Running
👥 Connected Clients: 45
⬆️  Upload: 2.3 GB
⬇️  Download: 1.2 GB
⏱  Uptime: 2 days 5 hours
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 You're helping people access freedom
```

<div dir="rtl">

### دستورات جدید نسخه ۲.۰

</div>

```bash
conduit history     # نمایش روند ۷ روز اخیر با نمودار
conduit summary     # خلاصه تأثیرگذاری هفتگی
conduit export      # خروجی گرفتن از داده‌ها به فرمت CSV
conduit telegram    # مدیریت اعلان‌های تلگرام
```

<div dir="rtl">

**مثال خروجی `conduit history`:**

</div>

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📈 7-Day Trends
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Total Data Points: 168 hours
Peak Clients: 68
Total Upload: 23456 MB
Total Download: 12345 MB

Daily Trend (average clients):
Jan 25: ████████████ 48
Jan 26: ██████████ 42
Jan 27: ████████████████ 61
Jan 28: ███████████████ 58
Jan 29: █████████████ 52
Jan 30: ██████████████ 55
Jan 31: ████████████████████ 68
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

<div dir="rtl">

**مثال خروجی `conduit summary`:**

</div>

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🕯️  Weekly Impact Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
This week you helped:
  👥 ~680 people access the free internet
  📊 Served 35.1 GB of uncensored data
  ⏱ 168 hours of uptime

💡 Every byte counts. Thank you for your contribution
   to internet freedom.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

<div dir="rtl">

---

## آمار تاریخی (Historical Tracking)

نسخه ۲.۰ به طور خودکار آمار ایستگاه شما را هر ساعت ذخیره می‌کند و تا ۹۰ روز نگهداری می‌کند.

### چگونه کار می‌کند؟

۱. هر ساعت، یک اسکریپت خودکار (`conduit-history`) اجرا می‌شود
۲. آمار فعلی از فایل `stats.json` خوانده می‌شود
۳. یک اسنپ‌شات جدید به فایل `history.json` اضافه می‌شود
۴. حداکثر ۲۱۶۰ رکورد (۹۰ روز) نگهداری می‌شود

### مشاهده تاریخچه

</div>

```bash
conduit history     # نمودار ۷ روز اخیر
conduit summary     # خلاصه هفتگی
conduit export      # دانلود فایل CSV
```

<div dir="rtl">

**فایل CSV شامل این ستون‌هاست:**
- `timestamp`: زمان دقیق (Unix timestamp)
- `datetime`: تاریخ و ساعت قابل خواندن
- `clients`: تعداد کاربران متصل
- `upload_mb`: حجم آپلود (مگابایت)
- `download_mb`: حجم دانلود (مگابایت)
- `uptime_minutes`: زمان فعال بودن (دقیقه)

---

## اعلان‌های تلگرام (اختیاری)

می‌توانید یک ربات تلگرام تنظیم کنید تا گزارش‌های روزانه و هشدارها را دریافت کنید.

### راه‌اندازی تلگرام

**مرحله ۱: ساخت ربات**
۱. به [@BotFather](https://t.me/BotFather) در تلگرام مراجعه کنید
۲. دستور `/newbot` را بفرستید
۳. نام و username برای ربات انتخاب کنید
۴. **Bot Token** را کپی کنید (مثل: `123456:ABC-DEF...`)

**مرحله ۲: پیدا کردن Chat ID**
۱. به [@userinfobot](https://t.me/userinfobot) مراجعه کنید
۲. دستور `/start` را بفرستید
۳. **Chat ID** خود را کپی کنید (یک عدد مثل: `123456789`)

**مرحله ۳: تنظیم در Conduit**

</div>

```bash
conduit telegram setup
```

<div dir="rtl">

Token و Chat ID را وارد کنید.

**مرحله ۴: تست کردن**

</div>

```bash
conduit telegram test
```

<div dir="rtl">

اگر پیام تست را دریافت کردید، تنظیمات موفقیت‌آمیز بوده است! ✅

### نوع اعلان‌ها

**گزارش روزانه (هر روز ساعت ۸ صبح):**

</div>

```
🤖 Conduit Daily Report

📊 Last 24 Hours:
👥 Clients: 45 (peak: 68)
⬆️ Upload: 3.2 GB
⬇️ Download: 1.8 GB
⏱ Uptime: 100%

💚 Keep running! You're making a difference.
```

<div dir="rtl">

**هشدار قطعی (هر ۳۰ دقیقه چک می‌شود):**

</div>

```
⚠️ Conduit Alert

Your Conduit container has stopped.
Please check: conduit logs

Server: your-server-hostname
Time: 2026-01-31 14:30 UTC
```

<div dir="rtl">

### مدیریت تلگرام

</div>

```bash
conduit telegram setup    # تنظیم یا تغییر اطلاعات
conduit telegram test     # ارسال پیام تست
conduit telegram disable  # غیرفعال کردن اعلان‌ها
```

<div dir="rtl">

**نکات امنیتی:**
- اطلاعات ربات در فایل `telegram.conf` با دسترسی محدود (chmod 600) ذخیره می‌شود
- هیچ داده‌ای به سرویس‌های خارجی ارسال نمی‌شود (به جز Telegram API)
- Bot Token را با کسی به اشتراک نگذارید

---

## ساختار فایل‌ها

مسیر پیش‌فرض نصب `~/conduit-docker` است:

</div>

```
conduit-docker/
├── docker-compose.yaml       # تنظیمات Docker و محدودیت منابع
├── conduit-data/             # پوشه نگهداری داده‌ها (بسیار مهم - پشتیبان بگیرید)
│   ├── stats.json            # آمار زنده (توسط Conduit ساخته می‌شود)
│   ├── history.json          # تاریخچه ۹۰ روز اخیر (نسخه ۲.۰)
│   ├── telegram.conf         # تنظیمات تلگرام (اگر فعال باشد)
│   └── conduit_key.json      # هویت پل شما در شبکه سایفون
└── install.sh                # اسکریپت نصب‌کننده
```

<div dir="rtl">

**مهم:** حتماً از پوشه `conduit-data` پشتیبان بگیرید! این پوشه شامل هویت منحصربه‌فرد ایستگاه شما و تمام آمار تاریخی است.

---

## سوالات متداول (FAQ)

### چطور روندهای آماری را مشاهده کنم؟

</div>

```bash
conduit history    # نمودار ۷ روز اخیر
conduit summary    # خلاصه هفتگی
```

<div dir="rtl">

**نکته:** آمار تاریخی از ساعت اول بعد از نصب شروع به جمع‌آوری می‌شود. برای دیدن روندها، حداقل چند ساعت صبر کنید.

### چطور داده‌ها را خروجی بگیرم؟

</div>

```bash
conduit export
```

<div dir="rtl">

یک فایل CSV در پوشه home شما ساخته می‌شود که می‌توانید با Excel یا Google Sheets باز کنید.

### چطور تلگرام را راه‌اندازی کنم؟

مراحل کامل در بخش [اعلان‌های تلگرام](#اعلان‌های-تلگرام-اختیاری) توضیح داده شده است. به طور خلاصه:

۱. از @BotFather یک Bot Token بگیرید
۲. از @userinfobot شناسه Chat ID خود را بگیرید
۳. دستور `conduit telegram setup` را اجرا کنید

### چه داده‌هایی ذخیره می‌شود؟

فقط این اطلاعات به صورت محلی روی سرور شما ذخیره می‌شود:
- تعداد کاربران متصل در هر ساعت
- حجم ترافیک آپلود و دانلود
- مدت زمان فعال بودن سرویس

هیچ اطلاعات شخصی یا شناسایی‌کننده کاربران ذخیره نمی‌شود.

### آیا داده‌های من امن هستند؟

بله! تمام داده‌ها فقط روی سرور شما ذخیره می‌شوند:
- هیچ اطلاعاتی به سرویس‌های خارجی ارسال نمی‌شود
- فقط شما به این داده‌ها دسترسی دارید
- تنها استثنا: اگر تلگرام را فعال کنید، گزارش‌ها از طریق API تلگرام ارسال می‌شوند

### چقدر فضای دیسک مصرف می‌شود؟

- فایل `history.json`: حدود ۱-۲ مگابایت (۹۰ روز داده)
- لاگ‌های Docker: حداکثر ۳۰ مگابایت (محدود شده)
- کل مصرف: کمتر از ۱۰۰ مگابایت

### اگر سرور را Reboot کنم چه می‌شود؟

نگران نباشید! تمام چیز خودکار راه می‌افتد:
- Docker خودکار شروع می‌شود
- Container به طور خودکار اجرا می‌شود
- جمع‌آوری آمار ادامه پیدا می‌کند
- هیچ کاری لازم نیست انجام دهید

### چطور از آمار تاریخی پشتیبان بگیرم؟

</div>

```bash
# کپی کردن پوشه کامل
cp -r ~/conduit-docker/conduit-data ~/conduit-backup

# یا فقط فایل‌های مهم
cp ~/conduit-docker/conduit-data/history.json ~/history-backup.json
cp ~/conduit-docker/conduit-data/conduit_key.json ~/key-backup.json
```

<div dir="rtl">

---

## رفع مشکلات (Troubleshooting)

### ایستگاه راه نمی‌افتد

</div>

```bash
# چک کردن وضعیت
docker ps -a

# دیدن لاگ‌ها
conduit logs

# ریستارت کردن
conduit restart
```

<div dir="rtl">

### آمار نمایش داده نمی‌شود

</div>

```bash
# چک کردن فایل stats
cat ~/conduit-docker/conduit-data/stats.json

# اگر فایل خالی است، چند دقیقه صبر کنید
# سپس دوباره امتحان کنید
conduit report
```

<div dir="rtl">

### تلگرام کار نمی‌کند

</div>

```bash
# تست مجدد
conduit telegram test

# اگر خطا داد، تنظیمات را دوباره وارد کنید
conduit telegram setup
```

<div dir="rtl">

### جمع‌آوری تاریخچه کار نمی‌کند

</div>

```bash
# چک کردن cron jobs
crontab -l

# اجرای دستی
/usr/local/bin/conduit-history

# چک کردن نتیجه
cat ~/conduit-docker/conduit-data/history.json
```

<div dir="rtl">

---

## به‌روزرسانی از نسخه ۱.۰ به ۲.۰

اگر قبلاً نسخه ۱.۰ را نصب کرده‌اید:

</div>

```bash
# ۱. رفتن به پوشه پروژه
cd ~/conduit-docker

# ۲. دانلود نسخه جدید install.sh
curl -fsSL https://raw.githubusercontent.com/netadminplus/conduit-docker/main/install.sh -o install.sh

# ۳. اجرای مجدد نصب (داده‌های قبلی حفظ می‌شوند)
sudo bash install.sh
```

<div dir="rtl">

**نکته:** هویت و کلید قبلی شما حفظ می‌شود و نیازی به تنظیم مجدد نیست.

---

## پشتیبانی

- 📺 **یوتیوب**: [@netadminplus](https://youtube.com/@netadminplus)
- 🌐 **وبسایت**: [netadminplus.com](https://netadminplus.com)
- 📸 **اینستاگرام**: [@netadminplus](https://instagram.com/netadminplus)
- 🐛 **گزارش مشکل**: [GitHub Issues](https://github.com/netadminplus/conduit-docker/issues)

---

## لایسنس

MIT License - استفاده و تغییر آزاد است

---

## سازنده

**رامتین رحمانی‌نژاد - نت ادمین پلاس**

کمک به جامعه ایرانی برای استقرار ابزارهای ارتباطی متن‌باز

[یوتیوب](https://youtube.com/@netadminplus) • [وبسایت](https://netadminplus.com) • [اینستاگرام](https://instagram.com/netadminplus)

---

## حمایت از پروژه

اگر این پروژه به آزادی اینترنت کمک کرد:

- ⭐ به این ریپازیتوری ستاره بدهید
- 📺 کانال [نت ادمین پلاس](https://youtube.com/@netadminplus) را سابسکرایب کنید
- 📢 این لینک را با ایرانیان خارج از کشور به اشتراک بگذارید
- 💬 تجربیات خود را در Issue ها یا کامنت‌های یوتیوب بنویسید

هر سروری که اضافه می‌شود، یعنی یک نفر بیشتر می‌تواند صدایش را به جهان برساند. 🕯️

---

**ساخته شده با ❤️ برای ایران**

**نسخه ۲.۰ - بهمن ۱۴۰۴**

</div>
